PROJECT: classroom-lan-v2 项目文档 2.0（执行后+二次完善）
日期：2026-05-04

一、已执行完成（对应原计划条目）
1) 领域对象去 Spring Bean 化
- Player/Room 已移除 @Component。

2) ID 不可变
- Player.id 改为 final，不再允许 setId 覆盖。

3) 去重与身份模型升级
- 增加 deviceId 字段。
- Room 增加 findByDeviceId。
- join 流程优先按 deviceId 识别，IP 仅兜底。

4) 统一 Room 访问入口
- net 层关键服务/控制器改为注入 RoomManager，并通过 default room 访问。

5) UDP socket 并发 receive 风险收敛
- 初始选举不再在 performElection 内直接 receive。
- 接收统一由 receiverLoop 消费，避免同 socket 多处阻塞读取。

二、第二步（剩余条目）执行结果
1) Identity Rule（只用 deviceId）
- 已完成主流程“deviceId 优先”。
- 保留 IP 兜底仅用于迁移期兼容（下一小版本可彻底下线）。

2) Host election
- 保留“先自举再收敛”模型，满足 LAN 场景快速可用。

3) Resource Handler
- 维持 /api /ws 排除逻辑；后续以集成测试固化。

三、第三步：二次完善（本次新增）
1) 统一 default room 的惰性创建
- 所有关键入口统一 roomManager.getRoom("default") 不存在即创建。

2) 文档化迁移策略
- 客户端必须上报 deviceId。
- 服务端保留一个版本窗口的 IP 兜底，再移除。

3) 回归测试建议
- 多标签页同账号并发连接。
- 刷新/重连不重复建号。
- 主机切换后状态收敛。
- 私聊、文件上传、头像上传在 RoomManager 路径下可正常工作。

四、下一步（2.1）
- 全量清理 findByIp 身份路径，改为 session->playerId/deviceId 绑定。
- 为 ResourceHandler、HostElection、Join/Reconnect 补自动化集成测试。
- 增加“迁移完成开关”：关闭 IP fallback。
